# Core ADK framework + A2A server
# a2a-sdk 1.0.0 (2026-04-20) is a breaking change — google-adk 1.31.x still
# imports from the pre-1.0 module layout (a2a.server.apps.A2AStarletteApplication).
# Pin both to the compatible pre-1.0 line until google-adk migrates.
google-adk>=1.31.0,<2.0.0
a2a-sdk[http-server]>=0.3.26,<1.0.0

# HTTP client for FHIR tools
httpx>=0.28.0

# Environment variable loading
python-dotenv>=1.0.0

# ASGI server
uvicorn>=0.41.0

# PDF parsing for prior operative notes
pypdf>=4.0
